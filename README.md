# Food Products Marketplace 🥬

Платформа за обяви на български хранителни продукти с **PWA (Progressive Web App)** функционалност.

## ✨ Характеристики

- 🛒 Обяви за хранителни продукти с множество снимки
- 📱 **PWA - Инсталируемо като мобилно приложение**
- 🔍 Филтриране по категории, региони и градове
- 👤 Потребителски профили и управление на обяви
- 🕒 30-дневна валидност с автоматично изтриване
- 🍪 GDPR съвместимост (Cookie consent)
- 🎨 Responsive Bootstrap 5 дизайн
- 🖼️ Поддръжка на до 5 снимки на обява
- 📞 Директни телефонни връзки

## 🚀 Бързо стартиране

### Prerequisites
- Python 3.13+
- Poetry

### Инсталация

1. Клонирайте проекта:
```bash
git clone <your-repo-url>
cd products
```

2. Инсталирайте dependencies:
```bash
poetry install
```

3. Създайте `.env` файл:
```bash
cp .env.example .env
```

4. Приложете миграциите:
```bash
poetry run python manage.py migrate
```

5. Създайте superuser:
```bash
poetry run python manage.py createsuperuser
```

6. Стартирайте dev server:
```bash
poetry run python manage.py runserver
```

Отворете браузър на http://127.0.0.1:8000

## 📁 Структура на проекта

```
products/
├── catalog/              # Основно приложение
│   ├── models.py        # Product, Category, Region, City, UserProfile
│   ├── views.py         # Всички view функции
│   ├── forms.py         # Django forms с български labels
│   ├── urls.py          # URL routing
│   └── templates/       # HTML templates
├── products/            # Django settings
│   ├── settings.py      # Конфигурация
│   ├── urls.py          # Главен URL conf
│   └── wsgi.py          # WSGI entry point
├── media/               # Качени файлове
├── staticfiles/         # Събрани статични файлове
└── manage.py            # Django management script
```

## 🎯 Функционалности

- ✅ Регистрация и автентикация
- ✅ Потребителски профили с снимка
- ✅ CRUD операции за продукти
- ✅ Качване на множество снимки
- ✅ Филтриране по категория/подкатегория и област/град
- ✅ Контакт форма
- ✅ Страници "За нас" и "Условия"
- ✅ Пълна българска локализация
- ✅ Адаптивен дизайн (Bootstrap 5)

## 🛠️ Технологии

- Django 6.0
- Bootstrap 5.3.8
- Python 3.13+
- SQLite (development) / PostgreSQL (production)
- Pillow (обработка на изображения)
- WhiteNoise (статични файлове)
- Gunicorn (production server)

## 📝 Development

### Създаване на миграции:
```bash
poetry run python manage.py makemigrations
poetry run python manage.py migrate
```

### Тестване:
```bash
poetry run python manage.py test
```

### Collectstatic:
```bash
poetry run python manage.py collectstatic
```

### PWA Икони:
```bash
poetry run python generate_pwa_icons.py
```

## 📱 PWA Функционалност

Сайтът е Progressive Web App и може да се инсталира на мобилни устройства!

### Как да инсталирате:

**Android (Chrome/Edge):**
1. Отворете сайта
2. Натиснете "Инсталирай" в banner-а
3. Или Menu → "Add to Home screen"

**iOS (Safari):**
1. Отворете сайта в Safari
2. Share → "Add to Home Screen"

**Desktop (Chrome/Edge):**
1. Иконата ⊕ в адресната лента
2. "Install app"

За повече информация: [PWA_GUIDE.md](PWA_GUIDE.md)

## 🚀 Deployment

Вижте [DEPLOYMENT.md](DEPLOYMENT.md) за пълни инструкции.

### Кратки стъпки:
1. Задайте environment variables в `.env`
2. Сменете на PostgreSQL база данни
3. Задайте `DEBUG=False`
4. Добавете домейн в `ALLOWED_HOSTS`
5. Настройте SSL (HTTPS)
6. Конфигурирайте email SMTP

## 📧 Контакт

Email: galinpavloveto@gmail.com

## 📄 License

MIT License
